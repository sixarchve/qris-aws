global:
  scrape_interval: 5s
  evaluation_interval: 15s

rule_files:
  - "/etc/prometheus/prometheus_rules.yml"

scrape_configs:
  - job_name: "qris-backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["golang:8080"]
